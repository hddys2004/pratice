//用C++的方式编程解决如下问题
//输入 3个 double 种类的值
//判断这了个值能否能够表示一个三角形的三条边如果能用海伦公式求三角形面积并输出。

//海伦公式p=（a+b+c）/2
//S = sqrt(p(p - a)(p - b)(p - c))
#include<iostream>
#include<math.h>
using namespace std;
int main()
{
	double a, b, c, p, s;
	cin >> a;
	cin >> b;
	cin	>> c;
	p = (a + b + c) / 2;
	if (s = sqrt(p*(p - a)*(p - b)*(p - c)))
	{
		cout << s << endl;
	}
	return 0;
}
